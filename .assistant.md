# Assistant Context Notes

## 2026-05-30 - Thesis workflow diagram guidance

- User is preparing Bab 4/Bab 5 thesis structure and a numerical implementation flowchart in Indonesian.
- Recommended diagram scope should represent the current full pre-surgery numerical workflow, not only the ODE steady-state loop: clinical data governance, patient-specific scaling, parameter registry/bounds, baseline ODE simulation with cycle convergence, optional PCE/Sobol GSA, governed calibration, validation gate, rollback/accepted candidate handling, and thesis/reporting exports.
- For thesis drafting, Mermaid flowchart code is a good source format because it is easy to revise and can be rendered/exported to SVG/PNG before final formatting.

## 2026-05-30 - Thesis patient characteristics table

- User asked for a Bab 3/Bab 4-ready patient table using only runnable pre-surgery cohort data.
- Cohort order for the table should follow the user's thesis grouping: Pasien 1 Reyna and Pasien 2 Razka as primer, then secondary runnable cases from `config/patient_cohort_cases.m`: Fathan, Azzam, Ali Zhafran, Salman, Syabil, Jericho.
- Table fields are sourced from patient config files: age, sex, weight, height, BSA, VSD location/type where explicitly available, and active `pre.VSD_diameter_mm`.
- Important caveat: Reyna and Razka configs do not expose a clear `VSD_location` field; Jericho reports a large VSD but visible diameter is unavailable, so use "tidak tercantum" or "tidak tersedia" rather than inventing values.
- User later requested the 4.2.2 main clinical target table from `outputs/pre_surgery_patient_workbook/MasterSheet_Hasil.xlsx` first, marking whether the best/calibrated model is within the 5% excellent-fit threshold. Use Section 3 clinical validation rows, not the model-derived section, to extract clinical value, BestModel, and BestError_pct.
- For the MasterSheet snapshot, excellent-fit counts across the main targets were: SAP_mean 5/8, PAP_mean 5/8, RAP_mean 1/3, CO_Lmin 1/4, QpQs 3/8.
- For thesis section 4.3.1, explain the initial baseline as a healthy adult reference parameter set from `config/default_parameters.m` (Valenti/Keisya baseline package, BSA reference about 1.73 m2) before patient-specific pediatric scaling. The saved Keisya/Reyna scaling comparison table is `results/scaling_method_comparison_summary_20260514.csv` plus `results/scaling_method_comparison/*/scaling_method_comparison_pre_surgery.csv`, using BSA 0.588 m2 in that snapshot. It compares Zhang weight-based scaling and Lundquist-BSA scaling; current primary workflow prefers Lundquist-BSA, while Zhang is a comparator.

## 2026-05-30 - Skripsi draft review notes

- User asked to evaluate `D:\Kuliah\Skripsi\Skripsi_Sidang.docx` without editing it.
- DOCX extraction found the draft currently has 898 paragraphs, 31 tables, and 60 media objects. Bab I-III mostly use Word heading styles, but Bab IV section labels are plain `Normal` paragraphs, so they should be converted to `Heading 1/2/3` before refreshing the table of contents.
- Bab IV currently has filled content mainly through 4.3.1 and tables 4.1-4.5; sections 4.3.2 onward are mostly outline placeholders and still need actual baseline simulation, GSA/PCE, calibration, validation, waveform/PV-loop, and discussion content from repo outputs.
- Review also found small text issues to fix: "Global Sensitvity Analysis" typo, "karaketeristik", "volumet", duplicate figure numbering in Bab III (`Gambar 3.1` and `Gambar 3.2` reused), and the 4.2.2 wording should not say "passed 5%" there because 4.2.2 is only clinical target data; pass/fail belongs in validation sections.
- Visual render QA could not be completed because the renderer could not find LibreOffice/soffice in the environment.

## 2026-06-01 - MasterSheet_Hasil expanded primary NRMSE recalculation

- User decided thesis-facing `primary_governed` should be expanded so it does not only contain mean pressure/flow targets.
- Updated `outputs/pre_surgery_patient_workbook/build_pre_surgery_patient_workbook_professionalized.mjs` so its output path is `outputs/pre_surgery_patient_workbook/MasterSheet_Hasil.xlsx`.
- The workbook now recalculates RMSE/NRMSE from existing validation CSV tables only; no MATLAB simulation is rerun.
- New `primary_governed` definition: `SAP_mean`, `PAP_mean`, `RAP_mean`, `CO_Lmin`, `QpQs`, `SAP_min`, `SAP_max`, `PAP_min`, `PAP_max` when finite clinical comparators are available.
- `SVR` and `PVR` remain out of `primary_governed` and stay in `full_transparent`/derived validation to avoid double-counting pressure-flow information.
- `hard_only` remains the stricter core objective set: `SAP_mean`, `PAP_mean`, `RAP_mean`, `CO_Lmin`, `QpQs`.
- `soft_only` is recalculated from `Q_shunt_Lmin`, `SAP_min`, `SAP_max`, `PAP_min`, `PAP_max`, `LAP_mean`, and `RVESV` when available.
- Rebuilt `MasterSheet_Hasil.xlsx` successfully and verified workbook sheet inventory, required sections, and formula-error scan.

## 2026-06-03 - Bab 3 numerical implementation workflow SVG

- Created thesis-facing SVG flowchart at `figures/thesis/diagram_implementasi_numerik_matlab.svg`.
- Diagram is intentionally compact for one-page Bab 3 placement and reflects the current MATLAB pre-surgery numerical workflow: clinical preprocessing, pediatric scaling, target governance, initial state setup, ODE steady-state loop, baseline output extraction, PCE/Sobol parameter screening, bounded calibration, validation gate, rollback, and reporting.
- Visual style follows the Q1 clean publication figures: white background, black typography, restrained blue/purple/orange accents, simple nodes, and minimal explanatory text.

## 2026-06-03 - Skripsi Bab 3 draft alignment review

- Reviewed `D:\Kuliah\Skripsi\Skripsi_Sidang.docx` Bab 3 without editing the document.
- Main alignment issues found: sample size still says five patients, 3.8.7 and 3.9.4 are Normal style instead of Heading 3, GSA text still reads like direct Monte Carlo Sobol instead of PCE/Sobol surrogate workflow, and RMSE table includes MAP even though the current expanded `primary_governed` workbook definition does not.
- Method text should also explicitly mention the current `UNIFIED_VSD_DO_GSA` optional toggle, PCE training sample configuration, validation gate/rollback behavior, and the updated expanded primary NRMSE definition from `MasterSheet_Hasil.xlsx`.

## 2026-05-21 - Reyna pre-surgery rerun after validation-governance update

- Latest rerun folder: `results/runs/20260521_205439_reyna_pre_surgery`
- Scenario: `pre_surgery`
- Patient: `reyna`
- Scaling mode: `lundquist_bsa`
- GSA and plotting were disabled for this rerun to focus on calibration and validation with the newest code changes.
- Calibration was not rolled back.
- Calibration status: `PHYSIOLOGICAL_BUT_POOR_FIT`
- RMSE:
  - primary governed baseline: `0.273767`
  - primary governed calibrated: `0.201976`
  - full transparent baseline: `0.420942`
  - full transparent calibrated: `0.358218`
  - hard-only calibrated: `0.130292`
  - soft-only calibrated: `0.113309`
- Primary gate:
  - accepted within 10 percent: `RAP_mean`, `SAP_mean`, `QpQs`
  - outside 10 percent: `PAP_mean` at `10.68%`, `CO_Lmin` at `25.98%`
- PVR and SVR are now handled as `derived_validation` metrics:
  - they remain visible in validation tables and error summaries
  - they are excluded from calibration and governed primary RMSE
  - latest accepted candidate errors: `PVR = 126.35%`, `SVR = 31.22%`
- Parameter plausibility: `12 OK`, `2 WARNING`, `0 FAIL`
- The remaining poor-fit driver is mainly systemic flow underprediction (`CO_Lmin = 2.5337 L/min` vs clinical `3.423 L/min`) and slightly high mean PAP (`16.60 mmHg` vs `15 mmHg`), not a rollback or hard physiology failure.

## 2026-05-21 - Reyna/Razka calibration failure diagnosis and governance fix

- Root cause of the bad-looking latest run:
  - Reyna was not failing because the simulation formula was broken or because rollback happened.
  - The optimizer objective and the reported validation RMSE were misaligned, and the current parameter registry over-constrained part of the old good basin.
  - Important bound mismatch found: BSA-scaled/reference values such as `C.PAR` and some `V0.*` seeds could sit outside the active feasible range, so the optimizer could not recover known-good physiology.
  - Sparse catheter cases such as Razka were being penalized by LVEDP/RVEDP even though no LV/RV volume anchors exist to identify passive chamber stiffness/preload from those pressures alone.
- Code changes made:
  - Added scaled-reference-in-bounds protection in `config/build_parameter_registry.m`.
  - Relaxed selected physiologic bounds for `V0.*`, atrial elastance, and RV elastance ceiling so strong candidates are warnings, not automatic hard failures.
  - Added Stage F validation-facing polish in `src/calibration/run_validation_gate_polish.m` and wired it from `src/calibration/run_calibration.m`.
  - Added `validation_holdout` target tier in `src/calibration/build_target_tiers.m`; sparse `LVEDP`/`RVEDP` are visible in full transparent validation but excluded from calibration and governed primary RMSE when no chamber volume anchors are available.
  - Updated calibration weighting/objective/gate polish to exclude `validation_holdout` like `derived_validation`.
- Latest accepted Reyna evidence:
  - Run folder: `results/runs/20260521_214941_reyna_pre_surgery`
  - Status: `ACCEPT`
  - RMSE primary governed: `0.058811`
  - RMSE full transparent: `0.155052`
  - Hard-only: `0.041028`
  - Soft-only: `0.025841`
  - Primary gate: all within 10 percent; only `RAP_mean` is outside the 5 percent excellent label at `5.48%`.
  - Remaining large errors are report-only/consistency issues: `RVEDV`, `LVESV`, and `LVEF` are `consistency_check_only`; `PVR`/`SVR` are `derived_validation`.
- Latest accepted Razka evidence:
  - Run folder: `results/runs/20260521_230800_razka_pre_surgery`
  - Status: `ACCEPT`
  - RMSE primary governed: `0.083017`
  - RMSE full transparent: `0.196714`
  - Hard-only: `0.042690`
  - Soft-only: `0.053866`
  - Primary gate: all within 10 percent; `RAP_mean` is outside the 5 percent excellent label at `6.49%`.
  - `LVEDP` and `RVEDP` remain large-error `validation_holdout` rows (`-28.77%`, `+49.91%`) because they are underidentified without LV/RV volume targets.
- Tests run after the fix:
  - `test_clinical_consistency_target_tiers`
  - `test_target_mapping_consistency`
  - `test_calibration_classification`
  - `test_parameter_registry`
  - `test_validity_gates`
  - `test_baseline`
- Interpretation for future sessions:
  - Use `primary_governed` RMSE for the accepted pressure-flow calibration result.
  - Use `full_transparent` RMSE to disclose all available reported/derived/holdout errors, not as the automatic accept/reject score.
  - For new patients, expect good error only when the clinical evidence supports identifiable targets. Sparse cath-only cases should fit direct pressure/Qp:Qs targets first and report EDP/volume/function as holdout unless supporting volume/function anchors are present.
- Git / branch note on 2026-05-22:
  - Rebasing `codex/baseline-integration` onto `origin/main` after PR conflict warnings hit the same governance files again.
  - Resolved by preserving the newer sparse-case governance choices: `Q_shunt_Lmin` remains a soft/reportable target, sparse cath weighting keeps `Q_shunt_Lmin` and `CO_Lmin`, and the branch should be pushed with `--force-with-lease` after rebase.

## 2026-05-24 - Reyna stable main-run recipe branch

- Worktree/branch: `D:\Kuliah\Skripsi\CollabHafizKeisya\unified_vsd_reyna_stable` on `codex/reyna-stable-main-run`, branched from `origin/main`.
- Added explicit recipe `config/calibration_recipes/reyna_pre_surgery.m` and recipe loaders/applicators so `main_run('pre_surgery', patient_reyna())` no longer infers Reyna calibration behavior from `NaN` clinical fields.
- Recipe-owned decisions:
  - demographics: 14.0 kg, 98.0 cm, BSA 0.6173419726
  - primary metrics: `RAP_mean`, `PAP_mean`, `SAP_mean`, `QpQs`, `CO_Lmin`
  - consistency-only: `LVEDV`, `LVESV`, `RVEDV`, `LVEF`
  - derived validation: `SVR`, `PVR`
  - `Q_shunt_Lmin` remains a soft calibration/reporting guard but is excluded from governed primary RMSE
  - active recipe parameters and accepted seed vector are pinned from `results/runs/20260521_214941_reyna_pre_surgery`
  - fixed accepted vascular/IC seed includes `V0.SVEN=421.1753037549494` and the accepted `params.ic.V` vector; this was the missing non-active seed that made the old accepted active vector fail on fresh `origin/main`.
- `main_run.m` now records `CalibrationRecipeId` and `CalibrationRecipeVersion` in `run_manifest.txt`.
- Verification:
  - Cheap tests passed: `test_reyna_systemic_flow_profile`, `test_clinical_consistency_target_tiers`, `test_parameter_registry`, `test_target_mapping_consistency`, `test_calibration_classification`.
  - Lundquist no-GSA, fast-off smoke completed at `results/runs/20260524_131114_reyna_pre_surgery`: `CalibrationStatus=ACCEPT`, `RollbackApplied=0`, primary governed RMSE `0.058804`, `CO_Lmin` error `3.756%`, all primary gates within 10%.
  - Zhang same-recipe run did not complete within interactive timeout. Its explicit recipe seed failed before optimization with RMSE `0.600054`, 4 primary-gate failures, and `CO_Lmin` error `66.22%`, which points to scaling/bounds/objective behavior rather than target recipe drift.

## 2026-05-24 — Stable main-run snapshot (Keisya baseline repair & scaling comparison)

- Branch: `codex/reyna-stable-main-run`, worktree at `unified_vsd_reyna_stable`.
- The explicit recipe `config/calibration_recipes/reyna_pre_surgery.m` is the source of truth for Reyna pre-surgery calibration: 14 active parameters, 5 primary pressure-flow targets, 4 consistency-only chamber rows, 2 derived-validation metrics.
- Demographics locked at the Keisya 2026-05-11 revision (14.0 kg, 98.0 cm, BSA 0.6173) through recipe application.
- Zhang scaling now matches Zhang 2019 Table 1: `Rvalve_open` exponent corrected to -0.50 (was -0.90); inertance L scaling added with eL = -1.0.
- Regression tests added:
  - `tests/test_reyna_hemodynamic_active_set.m` — asserts 14-param active set, bounds, and registry calibratable flags.
  - `tests/test_reyna_rmse_regression.m` — end-to-end Lundquist RMSE regression (< 0.10 primary governed; skips when `UNIFIED_VSD_RUN_HEAVY_TESTS` not set).
  - `tests/test_scaling_mode_parity.m` — heavy Lundquist-vs-Zhang same-governance comparison; Lundquist keeps the `< 0.10` RMSE gate, Zhang is reported as an independent comparator from its own baseline.
  - `tests/test_baseline_reference_metrics.m` — extended with provenance table smoke check.
- Cleanup: orphan `config/patient_profile_B.m` removed; virtual patients script updated.
- Target RMSE band: `primary_governed < 0.10` is required for the Lundquist accepted Reyna path. Zhang should be rerun and reported through the same recipe/governance path without borrowing the Lundquist disease seed.
- At that snapshot, all lightweight tests passed. After the later seed-separation/docs cleanup, rerun lightweight tests before committing if a fresh verification line is needed. End-to-end heavy tests must be run with `UNIFIED_VSD_RUN_HEAVY_TESTS=1`.

## 2026-05-24 - Zhang seed separation before stable-main merge

- Follow-up fix on `codex/reyna-stable-main-run`: the accepted Reyna disease seed from `results/runs/20260521_214941_reyna_pre_surgery` is now explicitly Lundquist-only.
- The shared `reyna_pre_surgery` recipe still covers both `lundquist_bsa` and `zhang` for demographics, targets, target tiers, active parameters, bounds, and reporting.
- Scaling-mode-gated blocks:
  - `recipe.initial_parameter_values.scaling_modes = {'lundquist_bsa'}`
  - `recipe.fixed_parameter_values.scaling_modes = {'lundquist_bsa'}`
  - `recipe.initial_conditions.scaling_modes = {'lundquist_bsa'}`
  - `recipe.accept_initial_seed_scaling_modes = {'lundquist_bsa'}`
- Effect:
  - Lundquist keeps the accepted seed path and can still short-circuit when the explicit seed passes the primary governed gates.
  - Zhang no longer inherits the Lundquist-calibrated disease vector, fixed `V0.SVEN`, or accepted IC vector. It starts from the Zhang-scaled healthy baseline plus normal clinical seeding, then uses the same Reyna governance for calibration.
- This was a code-only edit per user instruction. No MATLAB tests, smoke runs, or full pipeline runs were executed in this session.

## 2026-05-24 - Clinical validation vs model-derived reporting split

- Added Kei-inspired reporting split while keeping `codex/reyna-stable-main-run` canonical:
  - `validation_clinical_targets_baseline_<scenario>.csv`
  - `validation_clinical_targets_calibrated_<scenario>.csv`
  - `model_derived_metric_findings_<scenario>.csv`
  - `model_derived_parameter_findings_<scenario>.csv`
- The split is reporting-only. It does not change target governance, RMSE masks, calibration objective, Lundquist accepted seed behavior, or Zhang seed separation.
- Semantics:
  - clinical validation targets are rows with finite patient comparators; their tier still controls whether they are calibration, primary RMSE, consistency-only, derived-validation, or holdout evidence.
  - model-derived metric findings are model outputs without finite patient comparators; use them for physiologic interpretation, not patient RMSE.
  - model-derived parameter findings mirror the parameter plausibility table and should be compared to literature/plausibility bounds rather than direct patient measurements.
- No MATLAB tests, smoke runs, or pipeline runs were executed for this cleanup per user instruction.

## 2026-05-27 - Main branch fast-forward and temp ignore

- `main` was fast-forwarded to `origin/main` so the branch is no longer behind the remote.
- Added an exact ignore rule for `.tmp_zhang_paper.txt` in `.gitignore` on `main`, matching the same temporary-file handling used on the pipeline branch.
- The temp file should remain untracked in this worktree going forward.

## 2026-05-27 - Root documentation cleanup

- Moved root `AGENTS.md` into `.assistant/AGENTS.md` so project guardrails remain available without cluttering the repository root.
- Removed root-level administrative boilerplate files that the user wanted out of the main view: `CHANGELOG.md`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `PR_DESCRIPTION.md`, and `SECURITY.md`.
- Updated `README.md` to reflect the latest main-branch status, including Reyna recipe governance, Zhang seed separation, clinical-vs-model-derived reporting split, and the new `.assistant/AGENTS.md` location.
- Updated `docs/clinical_data_dictionary.md` references from `AGENTS.md` to `.assistant/AGENTS.md`.
- No MATLAB simulation source files were edited in this cleanup.

## 2026-05-27 - Pre-only cohort data pass

- User clarified their current work is pre-surgery only.
- Rechecked `patient_azzam.m` against `D:\Kuliah\Skripsi\Data Pasien\2. Muhammad Azzam Fayes.pdf`; active demographics, pressures, saturations, direct FR, worksheet-style Qp/Qs, and holdout notes are consistent with the source.
- Rechecked `patient_syabil.m` against `D:\Kuliah\Skripsi\Data Pasien\Syabil Muhammad Idrees.pdf`; active demographics, pressures, direct FR, Qp-derived Qs, and VSD dimensions are consistent with the source.
- Added `config/patient_jericho.m` from `D:\Kuliah\Skripsi\Data Pasien\jericho.docx`, which is image-scan based. Active Jericho pressure/EDP targets use the average of the three clearer Base O2 protocol measurements.
- Jericho has enough source evidence for pressure targets and saturation-derived `QpQs`, but absolute `CO_Lmin`, `Q_shunt_Lmin`, `PVR_WU`, and `SVR_WU` remain held out because O2 consumption and Hb are not visible in the provided scans.
- Updated `config/patient_cohort_cases.m` for the current VSD pre-only cohort: added `jericho`, excluded `ibnu_sina` because anthropometry/BSA are missing, and excluded `zoya` because the user clarified it is an ASD case.

## 2026-05-27 - Pre-surgery all-patient error dataset

- Ran latest pre-surgery error aggregation across the runnable VSD cohort: `reyna`, `razka`, `ali_zhafran`, `salman`, `fathan`, `azzam`, `syabil`, and `jericho`.
- Generated local analysis files under `results/analysis/pre_surgery_latest_20260527/`:
  - `pre_surgery_all_patient_metric_errors_latest.csv` with 208 patient-metric rows.
  - `pre_surgery_primary_metric_errors_latest.csv` with 71 primary/clinical target rows.
  - `pre_surgery_all_patient_rmse_summary_latest.csv` with one RMSE/status row per patient.
  - `README_pre_surgery_error_dataset.md` summarizing RMSE, latest run folders, and top primary errors.
- The aggregation uses `validation_baseline_pre_surgery.csv`, `validation_best_candidate_pre_surgery.csv`, `validation_rmse_summary_pre_surgery.csv`, `validation_primary_gate_pre_surgery.csv`, and `run_manifest.txt` from each patient's latest run folder.
- Important interpretation: `reyna` and `razka` remain accepted examples; the other cases are data-governance/disclaimer-heavy stress cases, mostly due to incomplete or contradictory flow/shunt anchors.

## 2026-05-27 - Professionalized pre-surgery patient validation workbook

- Rebuilt the all-patient workbook as `outputs/pre_surgery_patient_workbook/pre_surgery_patient_error_workbook_professionalized.xlsx`.
- Workbook sheets are `README`, `RMSE Summary`, and one patient sheet each for `reyna`, `razka`, `ali_zhafran`, `salman`, `fathan`, `azzam`, `syabil`, and `jericho`.
- Each patient sheet now uses the same seven-section structure: run metadata, RMSE summary, clinical validation metrics, target tiers/inclusion flags, primary/worst error findings, model-derived physiological values, and model parameter/plausibility findings.
- The workbook keeps clinical, baseline-model, and best-model values separated; unavailable source values are shown as `N/A` instead of being inferred.
- RMSE improvement and model-parameter percent change fields are formula-driven, and summary/patient tables use filters, frozen top rows, wrapped text, professional headers, and conditional formatting for review/high-error flags.
- Verification performed during export: workbook reopened successfully, all expected sheets were present, all mandatory patient sections were found, and the final formula-error scan found no `#REF!`, `#DIV/0!`, `#VALUE!`, `#NAME?`, or unintended `#N/A` cells.

## 2026-05-27 - R statistics companion for the pre-surgery workbook

- Added `scripts/analyze_pre_surgery_workbook.R` as a lightweight R entrypoint for the workbook statistics request.
- The script reads `outputs/pre_surgery_patient_workbook/MasterSheet_Hasil.xlsx`, summarizes the `RMSE Summary` sheet, and writes cohort tables plus a `primary_rmse_by_patient.png` plot under `outputs/pre_surgery_patient_workbook/r_stats/`.
- The intended use is quick reporting: patient counts, calibration-status counts, review-flag counts, and baseline-vs-best primary RMSE comparison.

## 2026-05-27 - Current Sobol setup check

## 2026-06-02 - Pre-to-post seed bundle request

- User asked for all available pre-to-post seed handoff files other than Reyna and Razka, bundled into one zip and renamed by patient plus scaling mode for easy comparison.
- Current seed source lives in each completed pre-surgery run under `results/runs/*_pre_surgery/mat/pre_to_post_seed_latest.mat`, with the run manifest in `run_manifest.txt`.
- For the bundle, prefer real named patients over placeholder profiles like `patient_profile_a`, `patient_profile_b`, and `patient_3p7kg_54cm` unless the user explicitly wants those test cases included too.
- Suggested filename pattern for the copied archive members: `<patient>_<scaling_mode>_pre_to_post_seed_latest.mat`.

## 2026-06-02 - CP1-kei branch check for Keisya post-prediction issue

- User asked whether Keisya's bad post-prediction result came from a mismatch between `origin/CP1-kei` and main.
- Compared `origin/main..origin/CP1-kei`: core pre-surgery model/orchestration file `main_run.m` is unchanged. Relevant differences are `run_post_surgery.m`, post-surgery target mappings/governance in `src/utils/get_calibration_targets.m`, `src/utils/params_from_clinical.m`, `src/calibration/build_target_tiers.m`, and a reporting print in `src/utils/validation_report.m`.
- Created separate worktree `D:\Kuliah\Skripsi\CollabHafizKeisya\unified_vsd_cp1_kei_check` at `origin/CP1-kei` to avoid touching the dirty main checkout.
- Ran CP1-kei Reyna pre-surgery with GSA/plots off. Result folder: `results/runs/20260602_182252_reyna_pre_surgery`; status `ACCEPT`; primary governed RMSE `0.058804`; full transparent RMSE `0.162387`; all primary metrics passed 10%.
- Ran CP1-kei Reyna post-surgery manually using the fresh pre-to-post seed and CP1 post targets with GSA/plots off. Result folder: `results/runs/20260602_182404_reyna_post_surgery`; status `PROMISING_NEAR_MISS`; primary governed RMSE `0.073717`; full transparent RMSE `0.0692`.
- The post run's only primary 10% failure was `QpQs`: clinical target `0.9`, model about `0.99996`, error `11.11%`. This is expected for a closed-VSD post model because `R.vsd` is forced closed and Qp/Qs tends toward 1.0; target `0.9` makes the gate structurally hard to pass.
- CP1 `run_post_surgery.m` also sets `UNIFIED_VSD_DO_GSA=1` despite the comment saying `0 biar ga pake GSA`; this can make Keisya's post run much slower and timeline-dependent, but the bad post fit is mainly target/governance driven, not a failed pre model.

- Live Sobol setup in `src/gsa/gsa_sobol_setup.m` now uses the default base sample count `N = 256` unless `GSA_SOBOL_N` or a function override is provided.
- For `pre_surgery`, the Sobol design includes 21 uncertain parameters total: 20 shared cardiovascular knobs plus the active shunt parameter (`R.vsd` or `vsd.Cd` depending on the patient setup).
- That means one Sobol pass evaluates `256 * (21 + 2) = 5,888` model runs before any calibration stage, and a full pre/post pair would double the Sobol cost unless an override is set.
- GSA is still an optional runtime toggle in `main_run.m`; it is useful for parameter screening and paper/reporting, but the accepted calibration path can run without it when you already know the active set or want faster turnaround.
- The live `main_run.m` GSA path is PCE-based (`gsa_pce_setup` + `gsa_run_pce`), not the older direct Sobol sampler; `gsa_pce_setup.m` currently uses `N_train = 128` Halton samples per GSA pass and shares that batch across all metrics.

## 2026-05-28 - Completed three-patient pre-surgery batch

- Completed the PCE-GSA pre-surgery batch for `reyna`, `razka`, and `ali_zhafran` with `N_train = 128`.
- `reyna`: `ACCEPT`, calibrated RMSE `0.056259`, full calibrated RMSE `0.162315`, rollback `0`.
- `razka`: `ACCEPT`, calibrated RMSE `0.067571`, full calibrated RMSE `0.172845`, rollback `0`.
- `ali_zhafran`: `PHYSIOLOGICAL_BUT_POOR_FIT`, calibrated RMSE stayed at `0.171870`, best-candidate RMSE `0.213677`, rollback `1`.
- Interpretation: `reyna` and `razka` are good accepted runs under the new PCE setting; `ali_zhafran` still does not improve under calibration and reverted to the baseline candidate.

## 2026-05-28 - Reyna filling-pressure cleanup

- User noticed `LAP_mean = 8` in the Reyna validation table even though LAP/PCWP was not directly measured.
- Root cause: `config/calibration_recipes/reyna_pre_surgery.m` overrode `pre.LAP_mean_mmHg = 8` and `pre.LVEDP_mmHg = 8` as validation-only filling estimates.
- Updated the Reyna recipe so both fields remain `NaN`; future Reyna runs will not count those estimated filling pressures in RMSE.
- No model rerun was performed. Recomputed from existing CSVs after excluding only Reyna `LAP_mean` and `LVEDP`: primary governed RMSE changed from `0.056259` to `0.045561`; full transparent RMSE changed from `0.162315` to `0.168619`.

## 2026-05-28 - Razka and Ali source check

- Checked `Procedure Log_VSD_Razka (3).pdf`: current Razka comparators match the source for error calculation (`RAP_mean=5`, `LVEDP=9`, `RVEDP=8`, `PAP=22/10/15`, `SAP=DAO 82/59/70`, `QpQs=1.21`). `LVEDP` and `RVEDP` remain validation holdout, not primary RMSE.
- Checked `Muhammad  Ali Zhafran  Mustofa.pdf`: pressure/QpQs comparators match except Ali had omitted direct filling pressures. Updated `config/patient_ali_zhafran.m` with `LVEDP=15`, `RVEDP=13`, and reported `SAP_mean=49` from `Ao pressure 72/37 (49)`.
- No model rerun was performed. Recomputed Ali from existing CSVs with corrected clinical comparators: primary governed RMSE `0.171331`, full transparent RMSE `0.294335`, holdout-only RMSE `0.535826`.

## 2026-05-28 - Razka run interpretation after secondary-data discussion

- Rechecked latest Razka run `results/runs/20260527_232456_razka_pre_surgery` without rerunning MATLAB.
- Manifest: `ACCEPT`, rollback `0`, primary governed RMSE `0.067571`, full transparent RMSE `0.172845`, hard-only `0.023240`, soft-only `0.045784`.
- Primary-gate rows are all excellent 5% fits: `RAP_mean` 4.39%, `PAP_mean` 0.96%, `SAP_min` 4.54%, `SAP_mean` 0.54%, `QpQs` 1.06%.
- Additional primary-included validation rows from direct cath pressures: `PAP_min` error 12.91% and `PAP_max` error -11.62%; these explain why primary RMSE is higher than the gate table but still accepted.
- `LVEDP` and `RVEDP` are direct cath measurements for Razka, but because there are no LV/RV volume anchors they remain `validation_holdout`: `LVEDP` 9 vs model 6.571 (-26.99%) and `RVEDP` 8 vs model 11.482 (+43.52%). These inflate full transparent RMSE only and should be discussed as secondary/underidentified filling-pressure evidence, not a strict model-failure criterion.

## 2026-05-28 - Q1-clean R figures for pre-surgery presentation

- User requested journal/presentation-quality figures from `outputs/pre_surgery_patient_workbook/MasterSheet_Hasil.xlsx` and existing `figures/R` outputs, explicitly using R rather than Python.
- Added `figures/R/figures_q1_clean.R`; it uses local R at `C:\Program Files\R\R-4.5.2\bin\Rscript.exe` because `Rscript.exe` is not on PATH.
- Generated final outputs under `figures/R/q1_clean_r/`: five figures with PDF, TIFF 600 DPI, and PNG versions, plus `index.html`, `README_Q1_clean_R_figures.md`, `source_rmse_summary_from_workbook.csv`, and `source_protocol_targets_from_workbook.csv`.
- Figure set:
  - `Q1_Fig1_reported_primary_rmse`: workbook-reported baseline vs best primary RMSE.
  - `Q1_Fig2_rmse_improvement`: percent improvement in reported primary RMSE.
  - `Q1_Fig3_protocol_target_agreement`: signed error for protocol-supported pressure-flow targets with a +/-10% band.
  - `Q1_Fig4_protocol_error_heatmap`: absolute percent error heatmap for protocol-supported targets, color capped at 100%.
  - `Q1_Fig5_rmse_governance_split`: primary-governed vs full-transparent RMSE to explain secondary/holdout effects.
- Added optional/supplementary R figures after user asked for more:
  - `Q1_Fig6_target_governance_map`: clinical target availability and tier map.
  - `Q1_Fig7_clinical_vs_model_scatter`: observed-vs-predicted scatter faceted by protocol-supported target.
  - `Q1_Fig8_patient_error_spread`: patient-level distribution of absolute protocol-target errors.
- Statistical/governance choice: target-level agreement figures exclude `validation_holdout`, `derived_validation`, `consistency_check_only`, unavailable rows, filling-pressure-only rows, and volume rows. They focus on protocol-supported pressure-flow targets only.
- Source check from generated CSVs: 8 patients total (`PASS=2`, `REVIEW=6`); target-level protocol dataset contains 67 patient-metric rows.

## 2026-05-29 - Calibration/objective technical explanation context

- User asked for a detailed explanation of what calibration/objective/scaling repairs produced the better results, including formulas and numbers.
- Key source files reviewed for the explanation: `src/calibration/objective_calibration.m`, `src/calibration/run_calibration.m`, `src/calibration/build_target_tiers.m`, `src/calibration/calibration_param_sets.m`, `src/utils/validation_report.m`, `src/utils/apply_physiological_scaling.m`, `src/calibration/build_case_calibration_profile.m`, and `config/build_parameter_registry.m`.
- Objective constants from `config/default_parameters.m`: primary target `5%`, secondary target `10%`, secondary lambda `1.0`, invalid penalty scale `1000`.
- Current objective form: primary/secondary normalized clinical residuals plus systemic bundle, pressure waveform, shunt-fraction, clinical guard, regularization, parameter-plausibility, boundary-plausibility, and validity penalties.
- Key calibration-governance repair: `validation_holdout`, `derived_validation`, and `consistency_check_only` are excluded from calibration objective and governed primary RMSE, while full transparent RMSE still reports them.

## 2026-05-30 - Old MATLAB prototype comparison

- User pointed to `D:\Kuliah\Skripsi\Matlab Code` and asked to compare it with the current repo.
- The old folder is an early exploratory prototype: 10 flat `.m` files, about 32 KB total, with 18 MATLAB function declarations. Important files are `run_valenti_vsd_demo.m`, `lpm_vsd_ode.m`, `lpm_postprocess.m`, `valenti_ref_params.m`, `teskesatu.m`, and `teskedua_scaling.m`.
- The old prototype already contains the core seed of the current work: Valenti-style lumped-parameter simulation, VSD shunt `Q_VSD = (P_LV - P_RV)/R_VSD`, Qp/Qs reporting, pediatric scaling experiments, and small hand-written calibration loops.
- The current `unified_vsd` repo is the source of truth: 125 non-toolbox MATLAB files across `config`, `src`, `scripts`, and `tests`, with about 615 function declarations plus docs, workbook/figure outputs, run manifests, governance, PCE/GSA, patient cohort profiles, pre/post scenarios, validation reports, rollback/candidate artifacts, and regression tests.
- Treat `D:\Kuliah\Skripsi\Matlab Code` as historical context/inspiration only. Do not port from it unless the user explicitly asks; compare against the current modular files such as `main_run.m`, `src/models/system_rhs.m`, `config/default_parameters.m`, and `config/patient_cohort_cases.m`.

## 2026-05-30 - Colebank arXiv VSD comparison

- User asked whether current `unified_vsd` is better than Colebank et al. arXiv PDF `C:\Users\muham\Downloads\2602.04008v1.pdf`, titled "Mathematical simulations of pediatric hemodynamics in isolated ventricular septal defect".
- Fair framing: Colebank is stronger as a published population/age-scaling physiology study; current repo is stronger as a patient-specific, governed calibration/audit pipeline for the user's clinical cohort.
- Colebank model: eight-compartment four-chamber ODE with diode valves, size-dependent/orifice-style VSD flow, CDC/Hiebing age-weight maturation from 1 day to 2 years, small/medium/large VSD cases (1/3/6 mm), PVR dysfunction scenarios, and 100-sample uncertainty propagation for maturation/blood volume.
- Colebank paper explicitly states a key limitation: it did not explicitly calibrate or validate to VSD subject data; it positions patient-specific calibration/validation as the next step toward digital shadows/twins.
- Current repo advantages: real patient profiles/cohort, target-tier governance, clinical consistency audits, parameter registry/bounds, Lundquist/Zhang scaling modes, PCE/GSA option, calibration classification/rollback, reproducible run folders, manifests, validation exports, pre/post handoff, tests, and thesis/workbook/figure outputs.
- Current repo weaknesses versus Colebank: not peer-reviewed, less polished as a mathematical paper, preschool extrapolation caveats for Reyna/Razka, and less systematic population growth/sex/age trajectory analysis than Colebank's 1-day-to-2-year framework.

## 2026-05-30 - Colebank comparison including skripsi manuscript

- User clarified the comparison should include both the written codebase and `D:\Kuliah\Skripsi\Skripsi_Sidang.docx`, not only the repo versus Colebank's paper.
- Current DOCX inspection via Word XML found about 1,253 non-empty paragraphs, 31 tables, 60 media objects, and about 20k words. Bab I-III are mostly styled with real Word headings, but Bab IV result sections from 4.3.2 onward are still mostly plain-text outline/placeholders rather than completed result prose.
- Keyword check: manuscript mentions calibration/validation/Qp/Qs/RMSE and has GSA/Sobol material, but does not currently mention Colebank by name, and Reyna/Razka patient names were not found in paragraph text. PCE appears mainly as a Bab IV placeholder.
- Fair judgment: as a codebase plus raw result infrastructure, the user's package is stronger than Colebank for patient-specific VSD calibration/audit. As a manuscript alone, Colebank is much stronger because the story, methods, figures, discussion, limitations, and citations are already integrated.
- Best improvement path: transfer the repo's mature artifacts into Bab IV/Bab V: baseline scaling, GSA/PCE setup/results, calibration objective and acceptance criteria, patient-level validation tables, Reyna/Razka accepted fits, poor-fit/holdout cases, waveform/PV-loop evidence, limitations, and direct comparison to Colebank as prior work/future-work bridge.

## 2026-05-31 - Branch-local improvement audit

- Created branch `codex/current-code-improvements` from the dirty `main` worktree so improvement work does not move the `main` branch pointer.
- Current active source changes before this audit already included Reyna filling-pressure cleanup (`LAP_mean_mmHg = NaN`, `LVEDP_mmHg = NaN`) and GSA sample-size tuning (`PCE N_train = 128`, direct Sobol default `N = 256`).
- Added `UNIFIED_VSD_GSA_PCE_N` as an optional environment override for PCE-GSA training sample count in `src/gsa/gsa_pce_setup.m`, documented it in `README.md`, and refreshed `main_run.m` comments so the live workflow says PCE/Sobol rather than older direct-Sobol wording.
- Verification: MATLAB `checkcode` passed for `src/gsa/gsa_pce_setup.m`, `src/gsa/gsa_sobol_setup.m`, and `config/calibration_recipes/reyna_pre_surgery.m`; full simulation/GSA was not run.
- Improvement candidates still worth considering later: decide whether generated `figures/` and `outputs/` artifacts should be intentionally tracked or ignored; keep thesis-facing workbook/figure outputs separate from source commits unless they are deliberate deliverables.

## 2026-05-31 - GSA cleanup and generated-artifact policy

- Decided root-level `figures/` and `outputs/` are generated local deliverable folders, not default source-controlled artifacts. `.gitignore` now ignores both roots; force-add only intentional release deliverables.
- Added `src/gsa/gsa_parameter_space.m` as the shared source of truth for GSA uncertain parameter names, nominal values, and bounds. `gsa_pce_setup.m` and `gsa_sobol_setup.m` both call this helper so PCE and direct Sobol cannot silently diverge.
- Cleaned `src/gsa/gsa_run_pce.m` comments that still referred to fixed 200-sample batches. The comments now describe `N_train` as configurable and emphasize the single shared ODE batch.
- Verification: MATLAB `checkcode` passed for the edited GSA files, and a helper smoke test with `default_parameters()` produced `pre=21` parameters and `post=20` parameters.

## 2026-05-31 - Publication-style GSA heatmap

- Reworked `src/gsa/make_gsa_matrix_table.m` from a dense table-style patch figure into a cleaner heatmap for Sobol total-order indices (`S_T`).
- The new figure sorts parameter rows by strongest total effect, uses a blue sequential color map, labels only high-impact cells or each row maximum, exports PNG/PDF/TIFF, and avoids TeX-subscript tick label issues.
- Verification used a synthetic GSA struct, not a full model rerun. MATLAB `checkcode` passed and smoke export wrote `gsa_st_heatmap_pre_surgery.[png|pdf|tiff]` under a temp directory.

## 2026-06-03 - Shared GSA parameter-space refactor

- Refactored the live GSA path so `src/gsa/gsa_parameter_space.m` now owns the shared uncertain-parameter list, nominal values, and bounds used by both PCE and direct Sobol setup helpers.
- Updated `main_run.m` comments and runtime notes to describe the active workflow as PCE/Sobol GSA, and exposed `UNIFIED_VSD_GSA_PCE_N` as a documented environment override in the runner and README.
- The refactor keeps the PCE and Sobol parameter definitions aligned so future parameter additions do not silently drift between the two sensitivity-analysis paths.
- Full simulation/GSA was not rerun in this session; the work is a source-level cleanup and documentation refresh around the current GSA pipeline.
